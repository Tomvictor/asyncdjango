version: '3'

services:
  db:
    container_name: postgres_async_container
    image: postgres
  redis:
    image: redis
    ports:
      - "6379:6379"
  web:
    container_name: asyncdjango-container
    build:
      context: .
      dockerfile: ./Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db